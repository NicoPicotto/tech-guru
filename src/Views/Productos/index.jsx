import { Stack, Heading } from "@chakra-ui/react";

const ProductosView = () => {
   return (
      <Stack>
         <Heading>Productos</Heading>
      </Stack>
   );
};

export default ProductosView;
